function initPage() {
    let logisticRegression = document.getElementById("logisticRegression");
    let test = logisticRegression.getElementsByClassName("entryStatus");
    test[0].innerHTML = "Clean";
}

initPage();